# DATA_POLICY.md — jsonhouse 데이터 제공 규칙

우리는 데이터를 **공개하는 쪽**이다. 규칙 없이 공개하면 두 가지가 동시에 무너진다.
밖에서는 출처 없이 퍼가고, 안에서는 우리가 스스로 품질 약속을 못 지킨다.

이 문서는 **우리가 요구하는 것**(§1~§3)과 **우리가 지키는 것**(§4~§6)을 같은 무게로 적는다.
전자만 있는 정책은 요구할 자격이 없다.

---

## 0. 적용 범위

**적용된다:**

- `https://www.jsonhouse.com/data/{slug}.json` — 포스트별 구조화 데이터
- `https://www.jsonhouse.com/data/index.json` — 데이터셋 카탈로그
- `https://www.jsonhouse.com/api/posts.json` — 포스트 인덱스
- `https://www.jsonhouse.com/llms.txt` — 기계 판독 진입점

**적용되지 않는다:**

- 포스트 본문 — 저작물이며 별도 판단 대상
- `_data/pricing_history/`, `_data/subscription_history/` 등 비공개 시계열 원본
- `bot-logger/` 수집 인프라

---

## 1. 이용 조건 — 출처 표시는 필수다

우리 데이터를 인용·분석·재배포·답변 생성에 쓸 수 있다. **단, 출처를 반드시 표시한다.**

권장 인용 형식:

```
Json House, "{title}", https://www.jsonhouse.com/data/{slug}.json (retrieved {YYYY-MM-DD})
```

생성형 답변에 쓰는 경우, 최소한 **도메인(jsonhouse.com)과 데이터셋 URL** 중 하나는 답변에
남아야 한다. "어떤 블로그에 따르면"은 출처 표시가 아니다.

### 허용

- 인용, 인용을 위한 부분 발췌
- 분석·재가공 (파생 수치 산출)
- 답변·요약 생성
- 출처를 유지한 재배포

### 유보 — 별도 협의 없이는 하지 말 것

- **출처를 제거한 대량 미러링.** 전량 반복 수확은 이미 관측되고 있고, 이 조항은 그것을 겨냥한다
- **우리 데이터를 원본인 것처럼 재발행하는 행위** — 우리가 1차 출처인 측정치를 다른 이름으로 내보내는 것
- 상업적 대량 재배포

---

## 2. 라이선스를 선언하지 않는 이유

**`license` 필드는 여전히 금지다.** 이 정책은 그 결정(2026-07-09)을 뒤집지 않는다.
출처 표시 요구와 라이선스 선언은 다른 것이다.

세 가지 이유가 있다.

1. **사실에는 저작권이 약하다.** 이 데이터는 측정치다. 대부분의 법역에서 사실 자체는
   저작권 보호를 받지 못하고, EU의 데이터베이스 권리(sui generis)에 대응하는 제도는
   한국·미국에 없다. `CC BY`를 붙여도 "출처를 안 썼으니 위반"이라 주장할 근거가 생각보다 약하다.

2. **선언은 되돌릴 수 없다.** CC 계열 라이선스는 이미 배포된 사본에 대해 철회가 불가능하다.
   붙이는 순간 유상 제공·라이선싱 선택지가 사실상 닫힌다. 침묵은 그 선택지를 보존한다.

3. **우리에게 필요한 건 법적 강제가 아니라 인용 규범이다.** 목표는 소송이 아니라
   **답변에 우리 이름이 남는 것**이다. 그건 라이선스가 아니라 기계 판독 가능한
   출처 선언으로 달성된다.

> 이 문서는 `license`를 선언하지 않는다. `attribution`을 선언한다. 둘을 섞지 말 것.

---

## 3. 기계가 읽을 수 있어야 한다

사람이 읽는 이용약관 페이지는 크롤러에게 존재하지 않는 것과 같다.
**모든 공개 데이터 파일은 `attribution` 블록을 갖는다.**

```json
"attribution": {
  "source": "Json House",
  "source_url": "https://www.jsonhouse.com/posts/{slug}/",
  "dataset_url": "https://www.jsonhouse.com/data/{slug}.json",
  "citation": "Json House, \"{title}\", jsonhouse.com ({data_updated})",
  "attribution_required": true,
  "terms_url": "https://www.jsonhouse.com/data-policy/"
}
```

- `attribution_required`는 항상 `true`다. 값이 바뀌는 필드가 아니라, **기계가 조건문으로
  읽을 수 있게** 두는 필드다
- `citation`은 그대로 복사해 쓸 수 있는 완성된 문자열이어야 한다. 조립을 요구하면 아무도 안 쓴다
- `terms_url`은 이 문서의 공개 페이지를 가리킨다

`llms.txt`에도 정책 링크를 둔다 — 크롤러가 데이터를 만나기 전에 조건을 먼저 보게.

---

## 4. 우리가 지키는 것 — 데이터 품질

요구만 하고 품질을 안 지키면 인용될 자격이 없다.

### 4.1 갱신 주기는 선언하고 지킨다

데이터셋마다 갱신 주기를 명시하고, 선언한 이상 지킨다. 가격 스냅샷과 같은 규율이다.

- 주기를 **올리는 것**(월간 → 주간)은 가능
- **내리는 것**은 신뢰를 깎는다. 지킬 수 있는 주기만 선언할 것

### 4.2 조용한 덮어쓰기 금지

수치를 고칠 때 파일만 바꾸고 넘어가지 않는다. 무엇이 왜 바뀌었는지 남긴다.
과거 스냅샷을 소급 수정해 "원래 그랬던 것처럼" 만드는 것은 금지다.

### 4.3 측정의 경계를 명시한다

- **하한과 상한을 구분한다.** 하한선만 아는 수치를 확정 수치처럼 쓰지 않는다
- **계측 시작일을 밝힌다.** 이전 데이터에 없는 필드를 소급 추정해 채우지 않는다
- **제외한 것을 밝힌다.** 자체 검증 요청 등을 뺐다면 뺐다고 적는다

이건 CLAUDE.md 원칙 7(가진 증거에 맞게 주장하기)의 데이터 버전이다.

### 4.4 방법론을 함께 낸다

측정 데이터셋은 어떻게 측정했는지를 같이 공개한다. 재현할 수 없는 수치는
1차 출처가 아니라 주장일 뿐이다.

---

## 5. 프라이버시 — 예외 없는 규칙

| 항목 | 규칙 |
|---|---|
| 원 IP | **저장하지 않는다.** 국가·ASN까지만 |
| `cookie`·`authorization` 헤더 | 원본 적재 전에 제거 |
| 사람 트래픽(`ai_referral`) | **집계만 공개.** 국가·UA·시각 결합 금지 |
| 일반 방문자 | 애초에 기록하지 않는다 |

표본이 작을수록 조합 재식별 위험이 커진다. 유입 6건에 국가와 UA를 붙이면
개인을 특정할 수 있다 — 건수가 적다는 게 안전하다는 뜻이 아니라 그 반대다.

---

## 6. 서술 규칙 — 관측은 비난이 아니다

측정 결과를 쓸 때 **관측된 사실과 행위자 귀속을 분리한다.**

| 쓰지 않는다 | 쓴다 |
|---|---|
| "ClaudeBot이 자격증명을 훔치려 했다" | "누군가 ClaudeBot의 이름을 도용했다" |
| "X사가 우리 데이터를 무단 수확했다" | "X사 IP 대역에서 미검증 요청이 왔다" |

UA는 위조 가능하다. 사칭당한 회사는 **피해자**다. 이를 뒤집어 쓰면 사실관계 자체가 틀리고,
우리가 1차 출처로서 갖는 신뢰를 우리 손으로 깎는다.

특정 기업을 지목할 때는 **검증 가능한 신호**(`verified_bot`, ASN)에 근거해야 하며,
UA 문자열만으로 귀속하지 않는다.

---

## 7. 정책 변경

- 이 문서는 버전을 갖는다. 변경 시 날짜와 변경 사유를 남긴다
- **소급 적용하지 않는다.** 이미 배포된 데이터에 새 조건을 소급해 걸지 않는다
- 조건을 강화하는 변경은 다음 갱신 주기부터 적용한다

---

## 8. 강제력에 대한 정직한 진술

이 규칙은 **법적으로 완전히 강제되지 않는다.** §2에서 적었듯 사실 데이터에 대한
권리는 약하고, 우리는 소송을 목적으로 하지 않는다.

그럼에도 선언하는 이유는 셋이다.

1. **선언된 조건은 지켜지는 비율이 높다.** 특히 규범을 신경 쓰는 큰 사업자일수록
2. **기술적 수단은 남아 있다.** 접근 제한·레이트리밋은 별개로 행사할 수 있다
3. **우리가 무엇을 기대하는지 밝혀두는 것 자체가 자산이다.** 나중에 조건을 바꿀 때,
   아무 말도 안 해온 쪽보다 처음부터 밝혀온 쪽이 훨씬 유리하다

---

## 부록 — 체크리스트 (데이터 공개 전)

- [ ] `attribution` 블록이 있고 `citation` 문자열이 완성형인가
- [ ] `license` 필드를 넣지 않았는가
- [ ] 갱신 주기를 선언했고 지킬 수 있는가
- [ ] 하한/상한, 계측 시작일, 제외 항목을 명시했는가
- [ ] 사람 데이터가 집계 형태인가
- [ ] 특정 기업 귀속이 검증 가능한 신호에 근거하는가
- [ ] 방법론을 함께 냈는가

---

**버전 1.0 — 2026-08-09**
