# Image Pack — best-llm-2026

## 대상

- 슬러그: `best-llm-2026`
- 포맷: A (도구 비교)
- 최종 이미지: 커버 1장
- 판단: 비교 수치와 결측값은 본문 표가 전달한다. 커버는 모델 선택 순서만 표현하며 수치를 시각화하지 않는다.

| 파일명 | 용도 | 주장 |
|---|---|---|
| `best-llm-2026-cover.png` | 커버 및 `og:image` | 모델은 입력·출력·캐시·수명이라는 네 운영 제약을 순서대로 확인해 선택해야 한다. |

## 근거 대응표

| 그림 요소 | 나타내는 것 | 본문 근거 | 실제 값 | 그림에 반영한 값 |
|---|---|---|---|---|
| 동일한 관문 4개 | 모델 선택 제약의 순서 | `How to Choose, by Constraint`의 4개 판단 문단 | 입력 형식 / 출력 크기 / 캐시 / 모델 수명 | 동일한 관문 4개 |
| 시작·종료 원의 동일성 | 동일 후보의 정체성 유지 | 네 제약을 같은 후보에 순서대로 적용한다는 본문 설명 | 동일 후보 | 같은 크기·색·형태의 원 |
| 관문 내부의 빈 공간 | 수치와 결측값을 커버에 넣지 않음 | 비교표에 실제 값 및 `Not published` 존재 | 커버에 표현하지 않음 | 빈 관문 |

## 커버

- 저장 경로: `assets/img/posts/best-llm-2026-cover.png`
- 규격: 1200×630 PNG, 200KB 이하
- alt: `Four operational checks narrow LLM choices without inventing values providers have not published`

### 생성 프롬프트

```text
STYLE: Flat vector, technical-editorial illustration.
Background #0F172A (deep navy), primary accent #38BDF8 (sky blue),
secondary accent #94A3B8 (slate), highlight #F8FAFC (near-white).
Thin 1.5px strokes, geometric shapes, generous negative space.
No gradients, no 3D render, no photorealism, no glossy highlights.
No hatching, no cross-hatching, no fill patterns — shapes stay unfilled or solid.
No human figures, no faces, no hands, no brains, no robots, no glowing orbs.
No text, no letters, no numbers anywhere in the image.
Clean, restrained, engineering-diagram feel.

SUBJECT: One unfilled circle follows a single straight line through exactly
four identical empty vertical gate frames and exits unchanged. The four gates
represent the post's four sequential operational checks without showing values.
COMPOSITION: Exactly four empty identical gates, evenly spaced in one centered
left-to-right flow, wide layout, generous margins.
ASPECT RATIO: 1200x630 (1.91:1)
CONSTRAINTS: Candidate circle identical at entrance and exit. Gate interiors
completely empty. No arrows. All line widths and gate sizes equal. Uniform
solid background.
AVOID: icons inside gates, funnels, clocks, layers, branch symbols, internal
marks, text, letters, numbers, pseudo-text, logos, bars, axes, scales, UI,
grids, hatching, patterns, glow, shadow, blur, gradients, texture.
```

## 검증

```bash
python3 .claude/hooks/optimize_image.py assets/img/posts/best-llm-2026-cover.png --cover
python3 .claude/hooks/image_validation.py --report
```
